const Button = () => {
  return (
    <button className="renewal-button" style={{ flex: 1 }}>
      <div className="renewal-button-text">Update Payment Details</div>
    </button>
  );
};

export default Button;
