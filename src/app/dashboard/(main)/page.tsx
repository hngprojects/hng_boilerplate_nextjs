import React from 'react'
import { Metadata } from 'next';

export const metadata:Metadata = {
  title: "Dashboard | HNG Boilerplate",
  description: "Access your personalized dashboard on the HNG boilerplate. Manage your account, settings, and activity here.",
};

const page = () => {
  return <div>page</div>
}

export default page
