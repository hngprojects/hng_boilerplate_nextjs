export type ProductTableProperties = {
  status: string;
  product_id: string;
  // product_id: string;
  name: string;
  price: number;
  description: string;
  // image: string;
  category: string;
  // status: "in_stock" | "out_of_stock" | "low_on_stock";
  quantity: number;
  // date_added: string;
  // time: string;
};
