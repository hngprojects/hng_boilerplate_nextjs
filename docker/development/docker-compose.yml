services:

  frontend:
    image: hngdevops/nextjs-boilerplate:dev
    ports:
      - 3100:3000
    volumes:
      - ../../.env:/app/.env

  # next-dev-frontend-2:
  #   build:
  #     context: ../../
  #     dockerfile: docker/development/Dockerfile
  #   ports:
  #     - 3130:3000
  #   restart: always
