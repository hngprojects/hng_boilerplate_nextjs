services:
  next-staging-frontend-1:
    build:
      context: ../../
      dockerfile: docker/staging/Dockerfile
    ports:
      - 3200:3000
    restart: always

  # next-staging-frontend-2:
  #   build:
  #     context: ../../
  #     dockerfile: docker/staging/Dockerfile
  #   ports:
  #     - 3230:3000
  #   restart: always